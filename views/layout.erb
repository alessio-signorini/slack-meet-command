<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @title || 'Slack Meet Command - Create Google Meet links from Slack' %></title>
  
  <% if ENV['GA_MEASUREMENT_ID'] && !ENV['GA_MEASUREMENT_ID'].empty? %>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=<%= ENV['GA_MEASUREMENT_ID'] %>"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '<%= ENV['GA_MEASUREMENT_ID'] %>', {
      'anonymize_ip': true
    });

    // Track OAuth completion on success page
    if (window.location.pathname === '/auth/success') {
      gtag('event', 'oauth_completed', {
        'event_category': 'authentication',
        'event_label': 'google_oauth'
      });
    }
  </script>
  <% end %>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      line-height: 1.6;
      color: #1d1c1d;
      background: #fff;
    }

    .header {
      border-bottom: 1px solid #e0e0e0;
      padding: 19px 0;
      background: #fff;
    }

    .header-content {
      max-width: 1040px;
      margin: 0 auto;
      padding: 0 32px;
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .app-icon {
      width: 64px;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #fff;
      border-radius: 12px;
      border: 1px solid #e0e0e0;
    }

    .app-icon img {
      width: 52px;
      height: 52px;
    }

    .header-info h1 {
      font-size: 28px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 4px;
    }

    .header-info .tagline {
      font-size: 16px;
      color: #616061;
      font-weight: 400;
    }

    .container {
      max-width: 1040px;
      margin: 0 auto;
      padding: 38px 32px;
    }

    .section {
      margin-bottom: 38px;
    }

    .section:last-child {
      margin-bottom: 0;
    }

    .section.intro {
      margin-bottom: 26px;
    }

    h2 {
      font-size: 20px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 13px;
      letter-spacing: -0.2px;
    }

    h3 {
      font-size: 16px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 10px;
      margin-top: 20px;
    }

    p {
      font-size: 15px;
      line-height: 1.7;
      color: #454245;
      margin-bottom: 13px;
    }

    ul, ol {
      margin-left: 24px;
      margin-bottom: 13px;
    }

    li {
      font-size: 15px;
      line-height: 1.7;
      color: #454245;
      margin-bottom: 8px;
    }

    code {
      background: #f8e5e5;
      color: #e01e5a;
      padding: 2px 4px;
      border-radius: 3px;
      font-family: Monaco, "Courier New", monospace;
      font-size: 13px;
      font-weight: 500;
    }

    a {
      color: #1264a3;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .command-box {
      background: #f8f8f8;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      padding: 16px 24px;
      margin: 19px 0;
      font-family: Monaco, "Courier New", monospace;
      font-size: 15px;
      color: #e01e5a;
    }

    .command-box .optional {
      color: #999;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 19px;
      margin-top: 19px;
    }

    .feature-item {
      padding: 0;
    }

    .feature-item h3 {
      font-size: 15px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 8px;
      margin-top: 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .feature-icon {
      font-size: 20px;
    }

    .feature-item p {
      font-size: 14px;
      line-height: 1.6;
      color: #616061;
      margin: 0;
    }

    .steps {
      margin-top: 19px;
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 19px;
    }

    .step {
      display: flex;
      gap: 16px;
    }

    .step-number {
      flex-shrink: 0;
      width: 32px;
      height: 32px;
      background: #1d1c1d;
      color: #fff;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      font-weight: 700;
    }

    .step-content h3 {
      font-size: 15px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 6px;
      margin-top: 0;
    }

    .step-content p {
      font-size: 14px;
      color: #616061;
      margin: 0;
    }

    .info-box {
      background: #f8f8f8;
      border: 1px solid #e0e0e0;
      border-left: 3px solid #007a5a;
      border-radius: 4px;
      padding: 20px 24px;
      margin-top: 24px;
    }

    .success-box {
      background: #f0fdf4;
      border: 1px solid #86efac;
      border-left: 3px solid #2eb67d;
      border-radius: 4px;
      padding: 20px 24px;
    }

    .success-box h2 {
      font-size: 20px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 13px;
      margin-top: 0;
    }

    .success-box p {
      font-size: 15px;
      line-height: 1.7;
      color: #454245;
      margin-bottom: 0;
    }

    .error-box {
      background: #fef2f2;
      border: 1px solid #fca5a5;
      border-left: 3px solid #ef4444;
      border-radius: 4px;
      padding: 20px 24px;
    }

    .error-box h2 {
      font-size: 20px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 13px;
      margin-top: 0;
    }

    .error-box p {
      font-size: 15px;
      line-height: 1.7;
      color: #454245;
      margin-bottom: 0;
    }

    .info-box h3 {
      font-size: 15px;
      font-weight: 700;
      color: #1d1c1d;
      margin-bottom: 8px;
      margin-top: 0;
    }

    .info-box p {
      font-size: 14px;
      line-height: 1.6;
      color: #454245;
      margin: 0;
    }

    .info-box ul {
      margin-top: 10px;
      margin-bottom: 0;
    }

    .back-link {
      display: inline-block;
      margin-bottom: 20px;
      color: #1264a3;
      text-decoration: none;
      font-size: 14px;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    .footer {
      border-top: 1px solid #e0e0e0;
      padding: 26px 0;
      margin-top: 51px;
      text-align: center;
    }

    .footer p {
      font-size: 13px;
      color: #616061;
      margin: 0;
    }

    .footer a {
      color: #1d1c1d;
      text-decoration: none;
      font-weight: 600;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .header-content {
        padding: 0 20px;
      }

      .container {
        padding: 26px 20px;
      }

      .features-grid {
        grid-template-columns: 1fr;
        gap: 32px;
      }

      .steps {
        grid-template-columns: 1fr;
      }

      .app-icon {
        width: 48px;
        height: 48px;
      }

      .app-icon img {
        width: 36px;
        height: 36px;
      }

      .header-info h1 {
        font-size: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-content">
      <div class="app-icon">
        <img src="/icon.png" alt="Slack Meet Command">
      </div>
      <div class="header-info">
        <h1><%= @page_title || 'Slack Meet Command' %></h1>
        <p class="tagline"><%= @page_tagline || 'Create Google Meet links instantly from Slack' %></p>
      </div>
    </div>
  </div>

  <div class="container">
    <%= yield %>

    <div class="footer">
      <p>
        <a href="/terms-of-service">Terms of Service</a> | 
        Built with 🤖 by <a href="https://alessiosignorini.com/" target="_blank" rel="noopener noreferrer">Alessio Signorini</a> |
        <a href="/privacy-policy">Privacy Policy</a>
      </p>
    </div>
  </div>
</body>
</html>
